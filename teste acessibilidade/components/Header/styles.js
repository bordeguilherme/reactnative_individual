import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  hdText: {
    color: "#f54472",
    fontSize: 30,
    fontWeight: "bold",
  },

  hdContainer: {
    paddingTop: 48,
    height: 135,
    justifyContent: "center",
    alignItems: "center",
  },
});
